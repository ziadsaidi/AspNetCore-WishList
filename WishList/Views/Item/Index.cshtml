@model List<WishList.Models.Item>

    <h1> WishList</h1>

    <a asp-action="create"> Add Item</a>

    <ul>
        @foreach (var item in Model)
        {
             <li>  @item.Description <a asp-action="delete" asp-route-id="@item.Id" >delete</a>
             </li>
 
            
        }
    </ul>